#include <stdio.h>
#include <math.h>

int main()
{
    int a = 10;
    while (a <= 20)
    {
        if (a >= 10)
        {
            printf("the value of 'a' is %d\n", a);
            a++;
        }
    }
    return 0;
}
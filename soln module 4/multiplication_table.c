#include<stdio.h>

#include <math.h>

    int main(){
    int x, y, result;
    x=1;
    printf("enter the number you want to get table of %d\n", y);
    scanf("%d",&y);
    for (int i = x ; i <= 10; i++)
    {
        printf("2 x table %d\n", y*i);
    }
   
    return 0;
}
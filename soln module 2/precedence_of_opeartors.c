#include <stdio.h>
#include <math.h>

int main()
{
    int x, y, z, k;
    x = 21;
    y = 36;
    z = 31;
    k = 23;
    printf("the value of 3*x/y-z+k --> this is the result '%d'", 3 * x / y - z + k);
    return 0;
}